# -*- dont-indent:t -*-
language: lisp

env:
  matrix:
    - VARIANT=v1 CUKE_ARGS="--tags ~@slow"
    - VARIANT=ref
    - VARIANT=v1

install:
    - bash travis/install.sh

script:
   - bash travis/script.sh
